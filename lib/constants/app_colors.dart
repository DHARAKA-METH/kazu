import 'package:flutter/material.dart';

class AppColors {
  // ☀️ Light Mode – White Base with Blue Highlights

  // Primary colors (for buttons, icons, active states)
  static const Color primary = Color(0xFF1976D2);      // Medium Blue
  static const Color primaryVariant = Color(0xFF0D47A1); // Deep Blue

  // Background & surfaces
  static const Color background = Color(0xFFF8FAFC);      // Very Light Gray    // Very Light Gray
  static const Color surface = Color(0xFFFFFFFF);   // Pure White
  static const Color surfaceSecondary = Color(0xFFF0F4F8); // Subtle Blue Tint

  // Text
  static const Color textPrimary = Color(0xFF1E293B);  // Dark Slate
  static const Color textSecondary = Color(0xFF64748B); // Muted Grayish Blue

  // Accent colors (for special UI elements)
  static const Color accent = Color(0xFF42A5F5);       // Light Blue Accent
  static const Color success = Color(0xFF16A34A);      // Green
  static const Color warning = Color(0xFFF59E0B);      // Amber
  static const Color error = Color(0xFFDC2626);        // Red

  // Borders & dividers
  static const Color divider = Color(0xFFE2E8F0);      // Light Gray

  // Btn Bacground
  static const Color btnBack = Color.fromARGB(255, 2, 1, 23);       // 
  static const Color btnTextPrimary = Color.fromARGB(255, 255, 255, 255);       // 
  static const Color btnTextSecondary = Color(0x00adb5bd);     // 

}
